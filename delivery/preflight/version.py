"""Stable identifiers embedded in the machine-readable report."""

# Bump the minor component on additive, backward-compatible schema changes and
# the major component on breaking changes. Consumers should pin on the major.
SCHEMA_VERSION = "tx10-clock/adb-preflight/v1"

TOOL_NAME = "adb-preflight"
