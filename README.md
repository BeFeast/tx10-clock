# TX10 Clock

After an approved GitHub Release is installed, TX10 Pro boots into an elegant analog + digital clock, stays stable, and shows the correct local time/date.

Project: `BeFeast/tx10-clock`.
