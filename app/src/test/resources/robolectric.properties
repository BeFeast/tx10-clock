# Declares the test environment for every Robolectric test in this module:
# API level 29 (matches min/target/compileSdk and the TX10 Pro runtime) with
# NATIVE graphics so Canvas/Bitmap render real ARGB_8888 pixels offscreen.
sdk=29
graphicsMode=NATIVE
