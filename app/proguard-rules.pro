# Release builds keep minification disabled (see app/build.gradle), so this file
# is intentionally empty. It exists so the release build type has a stable,
# reviewable ProGuard entry point if shrinking is enabled later.
